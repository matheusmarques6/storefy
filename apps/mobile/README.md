# apps/mobile — Fase 1

App Expo + expo-router + react-native-webview. **Ainda não implementado.**

Esta pasta existe para fixar a estrutura da seção 2 do `PLANO-DESENVOLVIMENTO.md`.
Ela é preenchida na **Fase 1 — App shell mobile**, seguindo a seção 5 do plano.

Não adicione código aqui antes da Fase 1: o `pnpm-workspace.yaml` só reconhece
pacotes com `package.json`, então esta pasta é ignorada pelo monorepo por enquanto.
